
// volume of cube => r*r*r;

#include <stdio.h>
void main()
{
    int radius;
    printf("Enter  the number");
    scanf("%d", &radius);
    radius = radius * radius * radius;
    printf("Radius of number = %d\n",radius);
}